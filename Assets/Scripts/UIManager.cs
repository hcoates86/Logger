using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UIManager : MonoBehaviour
{
    public RectTransform top50;
    public RectTransform bottom50;
    // Start is called before the first frame update
    // void Start()
    // {
    //     float newY = Screen.height - (Screen.height / 2);      // 50% 
    //     // top50.sizeDelta = new Vector2(top50.sizeDelta.x, newY);
    //     bottom50.sizeDelta = new Vector2(bottom50.sizeDelta.x, newY);
    //     Debug.Log(Screen.height);

    // }
}
